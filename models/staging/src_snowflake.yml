version: 2

sources:
  - name: raw
    database: MEDALLION_DB  # Explicitly set the database
    schema: raw
    tables:
      - name: customers_json
      - name: orders_csv
      - name: products_parquet
